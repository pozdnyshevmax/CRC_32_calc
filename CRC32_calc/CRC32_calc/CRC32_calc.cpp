#include "stdafx.h"
#include <iostream>
#include <string>
#include <vector>

using namespace std;

vector<string> tags;

int main(int argc, char **argv)
{
	if (argc < 2) {
		cout << "ERROR: need filename or mask" << endl;
	}
	
	system("pause");
	return 0;
}